__pycache__/
*.pyc
.venv/
.DS_Store

# Papers are fetched, not vendored: they are third-party PDFs.
reference/papers/

# Large third-party checkouts come in as submodules or via scripts/fetch_deps.sh
reference/rep_divergence/

# Modal volume pulls land here; only curated results are committed.
experiments/results/raw/

# MIB is fetched by scripts/fetch_deps.sh, not vendored or submoduled.
reference/MIB/
